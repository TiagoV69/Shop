spring.application.name=shop_api

# Configuracion de la conexión a la Base de Datos MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/tienda_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Mateito6900@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuracion de JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

spring.jpa.hibernate.ddl-auto=update

# Muestra en la consola el SQL que Hibernate está ejecutando util para depurar.
spring.jpa.show-sql=true